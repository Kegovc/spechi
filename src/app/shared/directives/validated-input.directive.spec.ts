import { ValidatedInputDirective } from './validated-input.directive';

describe('ValidatedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatedInputDirective();
    expect(directive).toBeTruthy();
  });
});
